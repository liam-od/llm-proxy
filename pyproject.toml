[project]
name = "llm-proxy"
version = "0.1.0"
description = "a LiteLLM proxy server with custom model handlers"
requires-python = ">=3.12"
dependencies = [
    "litellm[proxy]",
    "modal>=1.5.0",
]


[tool.pyright]
venvPath = "."
venv = ".venv"
