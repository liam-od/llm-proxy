[project]
name = "llm-proxy"
version = "0.1.0"
description = "a LiteLLM proxy server with custom model handlers"
requires-python = ">=3.12"
dependencies = [
    "litellm[proxy]",
]


[tool.pyright]
venvPath = "."
venv = ".venv"
